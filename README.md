# Dance Thru the Decades Portal v134 - HTML CSS Neon Hero

## Changes

- Rebuilds the hero effect using HTML/CSS rather than using a generated mock-up as the logo.
- Uses the real circular logo image again.
- Adds a CSS neon ring around the logo.
- Crops/scales the logo visually to reduce the harsh white rim.
- Removes the pulsing/shimmer bar from the 60s/70s/80s/90s/00s line.
- Makes the decades line cleaner with gold glow text.
- Adds neon side lines around THE.
- Adds extra glow/shadow around the main heading.
- Keeps clean deployment exclusions.

## SQL

No SQL changes.

## Suggested Git commit title

v134 HTML CSS Neon Hero
